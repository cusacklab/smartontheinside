# Runtime dependencies are declared in pyproject.toml; install with:
#   pip install -e .          (add [dev] for the test suite)
#
# This file is retained for the SLURM images that predate the packaging change.
boto3>=1.24
matplotlib>=3.5
nibabel>=4.0
numpy>=1.23
pandas>=1.4
scikit-learn>=1.1
scipy>=1.9
statsmodels>=0.13
